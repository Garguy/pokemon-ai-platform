mutation SubmitAnswers($answers: [AnswerInput!]!) {
    submitAnswers(answers: $answers) {
        id
        energy
        curiosity
        leadership
        loyalty
        risk
        creativity
        calculatedAt
    }
}
